[project]
name = "comfyui-dit-watermark"
version = "0.2.0"
description = "GROW frequency-guided DiT watermarking with Reed-Solomon detection for ComfyUI"
requires-python = ">=3.10"
dependencies = ["torch>=2.1"]

[tool.ruff]
line-length = 100

[tool.pyright]
pythonVersion = "3.10"
